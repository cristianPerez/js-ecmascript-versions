/**
 * We can generate an array with the string
 * values.
 */

const data = {
  frontend: 'oscar',
  backend: 'Cristian',
  design: 'laura',
};

const result = Object.values(data);
console.log(result);
console.log(result.length);