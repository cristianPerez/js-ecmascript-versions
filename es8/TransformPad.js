/**
 * We can concat values in the 
 * start and in the end of one string.
 */

const marketList = 'Market list';
console.log(marketList.padStart(24, '------').padEnd(38, '------'));

// expected answer
// -------------Market list--------------